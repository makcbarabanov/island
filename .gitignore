.env
__pycache__/
examples/
media/

# Cursor/VS Code: локальный settings.json из шаблона (.vscode/apply-cursor-env.sh)
.vscode/settings.json

# Локальная корзина (регламент — Readme/Readme.md, п. 13); не коммитим содержимое
_trash/

# Локальные/операционные бэкапы БД и архивы (вне git)
*.dump
db_backups*/
db_backups_safe/
*.tar.gz

# Экспорт Telegram — только локальный источник медиа
sites/breakfast/ChatExport_*/
sites/breakfast/.promo-screenshot.png

# Логи диалогов «Завтрак» (JSONL на диске, не в git)
logs/*.jsonl
logs/app.log
logs/app.log.*
!logs/.gitkeep

# Сгенерированный bundle для AI Studio (собирается скриптом)
Readme/CRONOS-upload-bundle.md

# Локальное venv
.venv/

# Одноразовые экспорты чатов (сумаризация — Readme/CRONOS-handoff.md)
chat/ai-studio-export-*.md