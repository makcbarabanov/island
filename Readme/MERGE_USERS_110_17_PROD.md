# Merge пользователей 110 → 17 (Щербинина Света, прод)

Дубликат: **110** «Светлана Щербинина» (тест `+79001110303`) — копия мечт без июльской активности.  
Канон: **17** «Света Щербинина» — buddy Макса, отчёты и шаги с 01.07.2026.

**Не путать:** id **33** — Светлана **Насонова**, отдельный активный участник марафона (импорт чата → id 33).  
id **17** — Света **Щербинина** (buddy Макса). Удаляется **только** id **110** (дубль Щербининой).

## Скрипт

`_sql/fix_merge_users_110_into_17.sql` — мечты 110 удаляются (дубликаты), user 110 удаляется.

## Продагент

```bash
cd ~/Apps/island && git pull --ff-only
test -f _sql/fix_merge_users_110_into_17.sql && echo OK
# бэкап по RUNBOOK при необходимости
psql -h "$DB_HOST" -p "${DB_PORT:-5432}" -U "$DB_USER" -d "$DB_NAME" \
  -v ON_ERROR_STOP=1 -f _sql/fix_merge_users_110_into_17.sql
```

Проверка: `SELECT id, name, surname, phone FROM users WHERE id IN (17, 110);` — только 17.
