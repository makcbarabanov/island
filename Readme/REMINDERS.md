# Напоминания Максу / Форжу

## ~2026-09-25 — Grok video API

Макс пользуется Grok на телефоне для **видео**; подписка в приложении ≠ API.

Когда будут свободны руки после приоритетных задач:

1. Аккаунт / ключ на [console.x.ai](https://console.x.ai) (`XAI_API_KEY`).
2. Smoke: один тестовый ролик через `POST /v1/videos/generations` (без UI).
3. Только потом — куда встроить в Остров (Тим / мечта / Завтрак).

Не путать с моделью Grok в Cursor (код).
