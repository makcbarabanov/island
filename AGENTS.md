# Агенты и окна Cursor (ОСТРОВ)

> **Канонический справочник ролей:** [Readme/AGENTS.md](Readme/AGENTS.md)  
> **Маршрутизатор личности:** `.cursor/rules/identity.mdc`  
> **Инструкция Продагента:** [Readme/Prodagent.md](Readme/Prodagent.md)  
> **Подсайты Острова (`/breakfast`, `/landing`):** [Readme/sites-architecture.md](Readme/sites-architecture.md)

## Почему можно «отправить не тому»

В двух окнах Cursor один и тот же репозиторий и похожий контекст. Среда **не** знает сама, «ты сейчас Forge или Продагент» — это задаётся **твоим выбором окна**, **включёнными правилами** и **тегом в сообщении**.

## Кто что делает

| Роль | Где | Код в репозитории | Git push |
|------|-----|-------------------|----------|
| **Forge** | Песочница (ноут Макса) | Да | Да → `main`; **«Деплой»** = push + SSH pull/rebuild на проде |
| **Продагент** | Прод (редко) | **Нет** | **Нет** — только если Форж не может или нужны прод-only ops |

Форж по команде **«Деплой»**: commit (если есть изменения) → push → `ssh makc@188.225.44.48` → pull + `docker compose up -d --build`. См. `.cursor/rules/island-deploy.mdc`.

Продагент (окно SSH + `[ПРОД]`): логи, smoke, откат, дампы — см. [Readme/RUNBOOK.md](Readme/RUNBOOK.md).

## Как жёстко не путать режимы

1. **Прод-окно Cursor:** включи правило **`.cursor/rules/island-prodagent-readonly.mdc`** (галочка в настройках правил для этого воркспейса).
2. **Пиши с тегом** `[ПРОД]` в **первой строке** каждого сообщения в этом окне (например: `[ПРОД] подтяни main и пересобери`).
3. **Окно Forge:** это правило **не** включай; тег `[ПРОД]` **не** используй.

Если Продагенту пришла задача на правку кода, правило заставит переспросить блоком **«МАКС, ЭТО ПРОД! …»** до ответа **`ДА ПРОД`**.

## Дополнительно

- Роли и границы: [Readme/PROJECT.md](Readme/PROJECT.md), корневой [README.md](README.md).
- Деплой: [Readme/RUNBOOK.md](Readme/RUNBOOK.md).
